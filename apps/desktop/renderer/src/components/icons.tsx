import type { LucideIcon } from "lucide-react";
import type { ReactElement } from "react";
import {
  AppWindow,
  Archive,
  ArrowLeft,
  ArrowRight,
  ArrowUpRight,
  Bookmark,
  Bot,
  Check,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  Circle,
  Clock,
  Cloud,
  Code,
  Columns2,
  Command,
  Cookie,
  Copy,
  CornerDownLeft,
  Cpu,
  Download,
  Ellipsis,
  ExternalLink,
  Eye,
  Folder,
  FolderOpen,
  Globe,
  Hand,
  Layers,
  LayoutGrid,
  List,
  Lock,
  MessageCircle,
  MessagesSquare,
  Moon,
  PanelLeft,
  PanelLeftClose,
  PanelRight,
  Pause,
  Pencil,
  Pin,
  Play,
  Plus,
  Printer,
  RotateCcw,
  RotateCw,
  ScanSearch,
  Search,
  Send,
  Settings,
  Snowflake,
  Square,
  Sun,
  Table,
  Trash,
  TriangleAlert,
  Unplug,
  User,
  X,
  Zap,
} from "lucide-react";

type IcoSize = "sm" | "md" | "lg";

const STROKE: Record<IcoSize, number> = { sm: 2, md: 1.75, lg: 1.75 };

function icon(Icon: LucideIcon, size: IcoSize = "md", fill = "none"): ReactElement {
  return (
    <span className={`ci ico-${size}`} aria-hidden>
      <Icon strokeWidth={STROKE[size]} fill={fill} />
    </span>
  );
}

/** Lucide stroke icons. Optical sizes: `--ico-sm` 14 / `--ico` 16 / `--ico-lg` 20. */
export const I = {
  back: icon(ArrowLeft),
  fwd: icon(ArrowRight),
  reload: icon(RotateCw),
  stop: icon(X),
  lock: icon(Lock, "sm"),
  globe: icon(Globe),
  globeLg: icon(Globe, "lg"),
  grid: icon(LayoutGrid),
  table: icon(Table),
  layers: icon(Layers),
  layersSm: icon(Layers, "sm"),
  download: icon(Download),
  gear: icon(Settings),
  gearSm: icon(Settings, "sm"),
  rail: icon(PanelRight),
  sidebar: icon(PanelLeft),
  sidebarClose: icon(PanelLeftClose),
  search: icon(Search),
  searchLg: icon(Search, "lg"),
  find: icon(ScanSearch),
  findSm: icon(ScanSearch, "sm"),
  chrome: icon(AppWindow, "lg"),
  chromeSm: icon(AppWindow, "sm"),
  clock: icon(Clock),
  clockSm: icon(Clock, "sm"),
  chat: icon(MessageCircle, "sm"),
  chats: icon(MessagesSquare),
  bookmark: icon(Bookmark),
  bookmarkFill: icon(Bookmark, "md", "currentColor"),
  agent: icon(Bot),
  agentSm: icon(Bot, "sm"),
  engine: icon(Zap, "sm"),
  cpu: icon(Cpu, "sm"),
  model: icon(Cpu, "sm"),
  hand: icon(Hand),
  handSm: icon(Hand, "sm"),
  person: icon(User),
  copy: icon(Copy, "sm"),
  check: icon(Check, "sm"),
  checkLg: icon(Check),
  pin: icon(Pin, "sm"),
  more: icon(Ellipsis),
  pencil: icon(Pencil, "sm"),
  trash: icon(Trash, "sm"),
  enter: icon(CornerDownLeft, "sm"),
  open: icon(ArrowUpRight, "sm"),
  circle: icon(Circle, "sm"),
  square: icon(Square, "sm"),
  play: icon(Play, "sm"),
  pause: icon(Pause, "sm"),
  close: icon(X),
  plus: icon(Plus),
  plusSm: icon(Plus),
  up: icon(ChevronUp, "sm"),
  down: icon(ChevronDown, "sm"),
  left: icon(ChevronLeft, "sm"),
  right: icon(ChevronRight, "sm"),
  send: icon(Send),
  obs: icon(Eye, "sm"),
  eye: icon(Eye, "sm"),
  run: icon(Play, "sm"),
  result: icon(List, "sm"),
  downloadSm: icon(Download, "sm"),
  alert: icon(TriangleAlert),
  command: icon(Command, "sm"),
  reloadSm: icon(RotateCw, "sm"),
  reopen: icon(RotateCcw, "sm"),
  sidebarSm: icon(PanelLeft, "sm"),
  sidebarCloseSm: icon(PanelLeftClose, "sm"),
  bookmarkSm: icon(Bookmark, "sm"),
  split: icon(Columns2, "sm"),
  folder: icon(FolderOpen),
  folderClosed: icon(Folder),
  archive: icon(Archive),
  print: icon(Printer, "sm"),
  code: icon(Code, "sm"),
  cookie: icon(Cookie, "sm"),
  unplug: icon(Unplug, "sm"),
  external: icon(ExternalLink, "sm"),
  sun: icon(Sun, "lg"),
  cloud: icon(Cloud, "lg"),
  moon: icon(Moon, "lg"),
  snowflake: icon(Snowflake, "lg"),
} satisfies Record<string, ReactElement>;
