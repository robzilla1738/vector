# VEC-021 End-to-end benchmark laboratory

**Acceptance:** record backend, security mode, hardware, failures. Cold/warm distributions in the engine perf tool. Stretch 2x p95 is a research target, not a gate.

**Evidence:** `tests/benchmarks/run.mjs` measures both backends, writes `heldOut` via `evaluateHeldOutAdvantage` (p95 of `act+observe.ms`) to `tests/benchmarks/reports/held-out-latest.json`. Latest both-backend run: Chromium p95 14.9 ms, engine p95 2.8 ms, p95Ratio 5.32, `successNonInferior` true. Tokens were not collected (`tokensPerSuccess` 0/0), so `meetsStretch` is false until a token-measured task suite is run. `evaluateHeldOutAdvantage` does not treat missing tokens as a free pass. `engine/tools/browserbench` pins JetStream (`c603c04…`) and enumerates every official Speedometer 3.0 suite from `tests.mjs` (32/32 executed, 0 NOTRUN). All 32 workloads are vendored from Speedometer `release/3.0`; `type=module` graphs are bundled to classic scripts; Handlebars `text/x-` templates are preserved. Suites execute with honest PASS/FAIL/PARTIAL (CI merge gate remains JetStream + `TodoMVC-JavaScript-ES5` + `motionmark.gpu.multiply`). MotionMark Multiply-class presents through `VelloRenderer::present_list` (GPU, no CPU readback; latest `adapter=Metal:Apple M5`). CI job `browserbench` (`--features v8,gpu`).

**Tests:** `pnpm bench --backend both`, `cargo run -p browserbench --features v8,gpu`, `tests/unit/roadmap-agent.test.ts`, `browserbench` `every_official_speedometer_3_workload_is_vendored`. Snapshots: `held-out-latest.json`, `browserbench-latest.json` in this directory.
