# VEC-021 End-to-end benchmark laboratory

**Acceptance:** record backend, security mode, hardware, failures. Cold/warm distributions in the engine perf tool. Stretch 2x p95 is a research target, not a gate.

**Evidence:** `tests/benchmarks/run.mjs` identity/hardware/securityMode/`requireIdentity`. `engine/tools/perf` reports backend, security_mode, os, arch, rss_bytes, host_package_energy_uj, layout_100 (Speedometer-class 100-item list), p50/p95.

**Tests:** `pnpm bench`, `cargo run -p perf -- --gate m1`.
