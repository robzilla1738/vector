# VEC-008 Web IDL bindings and JS/DOM object lifetime

**Acceptance:** author-created events cannot forge trusted state; prototypes; stale refs after slot reuse.

**Evidence:** harness `event.isTrusted-not-forgeable`. `bindings.rs` stale ref after recycle fails. DOM prelude wrappers.

**Tests:** `engine/crates/ve-agent/tests/bindings.rs`, wpt-harness.
