# VEC-007 Browser event loop and resource lifecycle

**Acceptance:** task sources, microtasks, virtual time, independent agents.

**Evidence:** `ve-script/src/event_loop.rs` task sources + `independent_loops_do_not_block_each_other`. Page `settle` drains fetches, event loop, V8 jobs, timers.

**Tests:** `ve-script` event_loop tests, `ve-agent` settle tests.
