# VEC-006 Real WPT execution and reporting

**Acceptance:** distinguish pass/fail/timeout/crash/not-run/skip. Pixel tests include images, text, clipping, opacity and painting. Script setup executes. Supported-suite regressions block merge. Testharness/IDL/fetch/storage fixtures run. Whole-tree HTTP, IDL harness, and reftest fonts are available.

**Evidence:** `wpt-runner` (geometry, pinned revision `7c20438…`, `m1.txt`). `--fonts-dir` / `--use-reftest-fonts` loads vendored `engine/conformance/fonts/Ahem.ttf` through `ParleyShaper` (not the merge gate; metric shaper remains the `m1` scorer). `wpt-harness` inlines pinned `testharness.js`, `WebIDLParser.js` / `webidl2.js`, and `idlharness.js`; `--http` serves fixtures plus `/resources` and `/fonts`; `--wpt-dir --tree --tree-family html/dom` walks every testharness HTML in the pinned checkout (`wpt-revision.txt` = `7c20438…`) and reports pass/fail/timeout/crash/skip/not-run. Latest full-family run: `wpt-tree-latest.json` — 302 PASS, 29 FAIL, 0 timeout/crash/skip, `tree_complete: true` (`tested_subset` 331 = 112 supported fixtures + official `html/dom`). Tree failures do not gate merge. Supported subset is `engine/conformance/testharness.txt` (114 files, merge-blocking). Official `html/dom/idlharness.https.html` PASS. Remaining official `html/dom` FAILs that are still disclosed: `aria-attribute-reflection-enumerated.tentative.html` missing-value defaults that are not null (string mixin `null` after remove is kept so `aria-attribute-reflection.html` stays PASS). Declarative partial updates (`template for`, `buffer`, `sanitize`, `streamAppendHTMLUnsafe`) and render-blocking cancel-on-remove are implemented. Official `nameditem-*.html`, innerText/outerText, `aria-element-reflection.html` / `aria-element-reflection-disconnected.html` / `aria-element-reflection-labelledby.html`, `historical.html`, `document.cookie`, `blocking-idl-attr.html`, lastModified, usvstring-reflection, `Document.currentScript.html`, `cdata-dir_auto.html`, leftover per-element IDL reflection, HTTP `:lang()`, `document.dir` / `dir-slots-directionality`, `self-origin.sub.html`, `name-content-attribute-and-property.html`, and `rel=expect` render-blocking (including 001/004/005/009/010/013/014/021) PASS.

**Tests:** `engine/fixtures/harness/*`, `wpt-harness` `http_server_serves_ahem_and_idlharness` / `inject_inlines_idlharness_and_webidl2`, `wpt-runner` `ahem_reftest_font_is_vendored` / `use_reftest_fonts_shapes_through_parley`, `ve-layout` `parley_shaper_registers_ahem_reftest_font`. CI `wpt` runs `--use-reftest-fonts --limit 8` against empty `reftest-fonts.txt` (not `m1.txt`).
