# VEC-006 Real WPT execution and reporting

**Acceptance:** distinguish pass/fail/timeout/crash/not-run/skip. Pixel tests include images, text, clipping, opacity and painting. Script setup executes. Supported-suite regressions block merge. Testharness/IDL/fetch/storage fixtures run.

**Evidence:** `wpt-runner` (geometry, pinned revision, `m1.txt`). `wpt-harness` inlines upstream `testharness.js` (same revision) into script fixtures, PNG-samples pixel tests (`data-sample-*`, `data-any-dark`), expected-failure metadata, overall_manifest = geometry + testharness. Upstream `document.title-01.html` is in the supported subset; `Document-getElementById.html` is listed in `expected-failures.txt`. CI jobs `wpt` and `wpt-harness`. Manifest: `engine/conformance/testharness.txt`.

**Remaining:** full WPT checkout (IDL, reftest fonts, WPT HTTP server), not only the supported subset.

**Tests:** `engine/fixtures/harness/*`, `wpt-harness` `fixture_http_server_serves_one_body`.
