# VEC-004 Build and test evidence

**Acceptance:** required checks run native addon/host, containment, fallback-disabled. Pin toolchain. Missing binaries are not green skips.

**Evidence:** `.github/workflows/engine.yml` rustc 1.88.0. Independent jobs: fmt, clippy, test, features (v8/http + product `ve-shell`), perf-gate, wpt geometry, wpt-harness (supported subset + `--tree --tree-family html/dom`), browserbench, addon + `ve-host`. Containment and broker tests are explicit steps.

**Tests:** the workflow itself.
