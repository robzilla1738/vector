# VEC-002 Production containment contract

**Acceptance:** negative tests for forbidden file reads, network creation, child-process attempts, malformed IPC, oversized messages, host termination. Production fail-closed.

**Evidence:** `docs/engine/containment.md`. `engine/crates/ve-host/tests/containment.rs` (file open, oversized IPC, `VECTOR_ENGINE_SANDBOX_SELFTEST=exec|network`). Production isolation `RequireProcess`. FD close loop in `sandbox.rs`. CI job `Negative containment (ve-host)`.

**Tests:** `cargo test -p ve-host --tests`.
