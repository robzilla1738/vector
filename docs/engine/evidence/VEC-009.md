# VEC-009 Fetch, streams, cancellation and web networking

**Acceptance:** async bodies (not blocking Promise wrappers), abort, clone/bodyUsed, WebSocket handshake/send/close.

**Evidence:** `fetchStart`/`fetchPoll`/`fetchAbort` completed in `settle`. AbortController. ReadableStream body view. RFC 6455 client in `ve-net/src/websocket.rs` including ping/pong, fragmented data frames, and TLS poll. Cross-origin non-simple script requests send OPTIONS and require `Access-Control-Allow-Methods` (`needs_preflight` / `check_preflight`).

**Tests:** `websocket.rs` `assembles_fragments_and_answers_ping` plus handshake/close, SW fetch intercept tests, bindings, `engine/fixtures/harness/websocket-close.html`.
