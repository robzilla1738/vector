# VEC-009 Fetch, streams, cancellation and web networking

**Acceptance:** async bodies (not blocking Promise wrappers), abort, clone/bodyUsed, WebSocket handshake/send/close.

**Evidence:** `fetchStart`/`fetchPoll`/`fetchAbort` completed in `settle`. AbortController. ReadableStream body view. RFC 6455 client in `ve-net/src/websocket.rs` with local listener test, `poll`/`take_incoming`, and script `open`/`close`/`message` listeners.

**Tests:** `websocket.rs` tests, SW fetch intercept tests, bindings, `engine/fixtures/harness/websocket-close.html`.
