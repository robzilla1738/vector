# VEC-018 Compiled guarded skills

**Acceptance:** reuse only when guards hold; failures stop at the guard.

**Evidence:** `apps/runtime/src/agent/skills.ts` `tryReuseSkill`. `evaluateHeldOutAdvantage` encodes the M3 stretch (2× p95, 50% fewer tokens, non-inferior success) and does not mark it met on weaker ratios.

**Tests:** `tests/unit/roadmap-agent.test.ts` VEC-018.
