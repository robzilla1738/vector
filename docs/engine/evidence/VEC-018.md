# VEC-018 Compiled guarded skills

**Acceptance:** reuse only when guards hold; failures stop at the guard.

**Evidence:** `apps/runtime/src/agent/skills.ts` `tryReuseSkill` + `verifySkillPostconditions`. Auto-compiled skills store `urlIncludes` postconditions. A reused skill that fails postconditions after execute escalates instead of claiming success. `evaluateHeldOutAdvantage` encodes the M3 stretch and does not mark it met on weaker or unmeasured tokens. Latest held-out snapshot (`held-out-latest.json`) is measured: `meetsStretch` true, `tokenRatio` 8.35 from declared model usage.

**Tests:** `tests/unit/roadmap-agent.test.ts` VEC-018.
