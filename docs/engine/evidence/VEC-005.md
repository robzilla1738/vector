# VEC-005 Capability evidence ledger

**Acceptance:** CSS.supports, matchMedia, fetch bodies, customElements, observers, WebSocket focused tests. No false advertising.

**Evidence:** `describe()` reports `websocket:true`, `streams:true`, `serviceWorkers:true` (persistent `SwRealm` isolate, `importScripts` at load, `clients.claim` → `navigator.serviceWorker.controller`, `clients.matchAll`, waiting-worker `skipWaiting`), `webgl:false`, `webgpu:false`, `http3:false`. Canvas `toDataURL` is a real PNG. `matchMedia` evaluates width/height/orientation/prefers-*. Ledger: `engine/conformance/capabilities.json`.

**Tests:** `engine/tools/wpt-harness`, `engine/crates/ve-agent/tests/bindings.rs` `match_media_orientation_and_compound_queries`, `engine/fixtures/harness/matchmedia-orientation.html`.
