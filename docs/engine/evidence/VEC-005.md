# VEC-005 Capability evidence ledger

**Acceptance:** CSS.supports, matchMedia, fetch bodies, customElements, observers, WebSocket focused tests. No false advertising.

**Evidence:** `engine/conformance/capabilities.json`. `describe()` reports `websocket:true` (RFC 6455 client), `http3:false`, `serviceWorkers:false`. WPT harness asserts CSS.supports (including compound queries), matchMedia listeners, Event.isTrusted, WebSocket close events.

**Tests:** `engine/tools/wpt-harness`, `engine/crates/ve-agent/tests/bindings.rs`.
