# VEC-003 Authoritative network and storage broker

**Acceptance:** forged identities, DNS rebinding, private-network, agent egress, credential stripping.

**Evidence:** `ve-net/src/broker.rs` stamps parent context (forged child id rejected). `policy.rs` revalidates resolved IPs, strips userinfo, default-blocks loopback/private, agent allowlist.

**Tests:** `cargo test -p ve-net --lib broker`, `policy` tests including `dns_rebinding_resolved_addresses`.
