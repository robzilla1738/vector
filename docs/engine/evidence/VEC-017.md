# VEC-017 Durable coordinator and fallback reconciliation

**Acceptance:** crash injection at transitions; no duplicate writes; specific expiring fallback reasons.

**Evidence:** `persistCheckpoint` on run status. `recoverAfterCrash` over `COORDINATOR_TRANSITIONS` never reissues writes (`duplicateWrites: 0`) and records `historicalDuplicates`. A restart with unresolved dispatched effects finishes `partially_completed` and asks for review instead of planning again.

**Tests:** `tests/unit/roadmap-agent.test.ts` VEC-017, `tests/unit/invariants.test.ts`.
