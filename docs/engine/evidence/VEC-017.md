# VEC-017 Durable coordinator and fallback reconciliation

**Acceptance:** crash injection at transitions; no duplicate writes; specific expiring fallback reasons.

**Evidence:** `persistCheckpoint` on run status. `recoverAfterCrash` over `COORDINATOR_TRANSITIONS`. `reconcileFallback` is not origin-wide ban.

**Tests:** `tests/unit/roadmap-agent.test.ts` VEC-017.
