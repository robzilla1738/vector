# VEC-011 Forms, trusted input and human takeover

**Acceptance:** takeover stops dispatch; already-dispatched effects reported, not claimed canceled.

**Evidence:** `pages.ts` marks `dispatchedBeforeTakeover` on conflict. `tests/integration/control.test.ts`, `tests/unit/engine-routing.test.ts` takeover. Executor stamps receipts.

**Tests:** those suites + `tests/unit/roadmap-agent.test.ts`.
