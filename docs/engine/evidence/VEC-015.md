# VEC-015 Versioned semantic query service

**Acceptance:** page/frame/document/generation identity; incremental queries match full recompute; token budgets.

**Evidence:** `EngineObservation.query_version`. `document_epoch` + revision. `changesSince` vs full snapshot. Observation budgets in `ve-a11y`.

**Tests:** ve-api observe/execute round trip, a11y observation tests.
